; 固定使用公共 npm registry，保证 package-lock.json 的 resolved 地址在
; CI（GitHub Actions 公网 runner）与任意开发机上均可解析。
; 历史问题：内网镜像（registry.anpm.alibaba-inc.com）写入 lock 后，
; 公网 CI 的 npm ci 会长时间重试并最终装不全依赖。
registry=https://registry.npmjs.org/
